name: Dev onboarding full tests

"on":
  push:
    branches:
      - "agent/dev-onboarding"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "runner ready"
